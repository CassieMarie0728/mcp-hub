# Infrastructure as Code Notes

Current repository provides starter Kubernetes manifests under `kubernetes/`.
Teams can replace or extend this with Helm/Terraform while keeping deployment contracts equivalent.
